# Signal Normalization

Normalization enriches qualified signals with:
- severity score
- confidence score
- deterministic dedupe hash

Normalization is engine-owned.
Downstream services must not mutate normalized fields.
