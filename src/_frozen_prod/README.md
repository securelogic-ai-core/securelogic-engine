# SecureLogic Engine – Public API

Consumers should only import from:

  import { SecureLogicAI } from "securelogic-engine";

All internal engines, builders, and renderers are private implementation details
and are not part of the public contract.
