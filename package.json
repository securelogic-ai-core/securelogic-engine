{
  "name": "securelogic-engine",
  "version": "0.1.6",
  "license": "MIT",
  "scripts": {
    "build": "tsc && npm run copy",
    "copy": "mkdir -p dist/frameworks dist/api && cp -r frameworks/* dist/frameworks/ && cp -r src/api/* dist/api/",
    "start:api": "node dist/api/server.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/jest": "^30.0.0",
    "@types/node": "^25.0.3",
    "jest": "^30.2.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.9.3"
  }
}