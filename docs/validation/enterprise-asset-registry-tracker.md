# Enterprise Asset Registry — Goal Tracker

Living tracker for the Enterprise Asset Registry workstream (design authority:
`docs/architecture/enterprise-asset-registry/ARCHITECTURE.md`). Successor workstream to the
completed Enterprise Context goal (`enterprise-context-goal-tracker.md`); same governing
invariants: everything DARK behind flags (default off, additive-only), branch off
`origin/develop`, squash-merge + delete branch, tenant scoping + inert RLS +
`dataClassification` on every new table, prod enablement out of scope (GATE B).

Last updated: 2026-07-06 (Phase 3a merged; Phase 3b built; scope expanded).

## Reconciliation notes (2026-07-05 resume)

- **Verified remote truth:** `origin/develop` tip = `592d9c60` (base ARCHITECTURE.md, Phases 0–5).
  No EAR feature branches on origin; no open EAR PRs; local `develop` in sync (0/0). All EAR
  code beyond that commit exists ONLY in the local working tree (Item 0, uncommitted).
- **Governance note:** `592d9c60` was pushed directly to `develop` without a PR (deviation from
  the squash-merge-via-PR convention; docs-only, recorded, not repeated).
- **EAR-AD-4 ruling record:** initially flagged GATE-VIOLATION-REVIEW (no ruling artifact in
  repo). **RESOLVED — Simmee ruling 2026-07-05:** authorized "Item 0 repair plan exactly as
  scoped" (commit → PR → CI → squash-merge), which ratifies EAR-AD-4 / Option 1. This entry is
  the ruling artifact.
- **Roadmap authority — Simmee ruling 2026-07-05:** governing scope is the **ratified repo plan**
  (option a): Item 0 → Phases 0–5 per ARCHITECTURE.md §4. Tracks A/B/C are adjacent/optional and
  must not block the core path. Anything beyond Phases 0–5: design/options memos only, stop at
  gates before building. Everything stays dark (`SECURELOGIC_ASSET_REGISTRY_ENABLED` default
  off); no production enablement; GATE B untouched.
- **Scope expansion — Simmee ruling 2026-07-06 (supersedes the "Phases 0–5 only" boundary above):**
  the Enterprise Asset Registry roadmap **P0–P11** is now the approved implementation scope.
  P0–P5 = the repo plan's Phases 0–5 (current execution sequence, unchanged). On completion,
  continue directly into **P6–P11** without restarting the goal; P6–P11 are not yet defined in
  the repo — a **design memo is required before implementing each**, with engineering/architecture
  decisions made autonomously where they preserve the governing principles. Mandatory stop
  conditions: product-vision decision, destructive migration, backward-compat break, operator/
  production action, true BLOCKED-ON-SIMMEE. Dark-by-default, backward compatibility, and
  no-production-enablement (GATE B) remain non-negotiable throughout.

## Ratified decisions

| ID | Decision | Rationale | Where |
|---|---|---|---|
| EAR-AD-1 | Federate, do not subsume | Registry references detail tables; preserves ECL AD-1/AD-13 | ARCHITECTURE.md §0 |
| EAR-AD-2 | Registry is identity-only; header is a view | Zero attribute duplication → no drift | ARCHITECTURE.md §2.1 |
| EAR-AD-3 | `asset_id` canonical join key; `(type,id)` stays for compat | Additive, quartet enums stop growing | ARCHITECTURE.md §2.1 |
| EAR-AD-4 | Asset/infrastructure edges live in `enterprise_relationships` (**Option 1**); NO new `enterprise_asset_relationships` table | A second edge table would split the graph, force a second resolver UNION arm + duplicate RLS/cap/audit surface, and contradict §3.1 (edge substrate = REUSE). ECL AD-3 makes new relationship kinds enum growth, not new tables. Resolver + AD-13 typed-edge union are vocabulary-agnostic → reused with zero changes. | ARCHITECTURE.md §2.4 |

## Item table

| # | Item | Status | Evidence | Flag / dark posture |
|---|---|---|---|---|
| 0 | EAR-AD-4 graph substrate expansion — widen `enterprise_relationships` endpoint CHECKs (+`asset`, schema-dark at route until Phase 1) and `relationship_type` (+`hosted_on`, `connects_to`, `stores_data_in`, `authenticates_via`, `exposed_via`, `managed_by`, live). Additive migration only; ECL context edges unchanged; RLS/unique-index/self-edge CHECK/edge cap/dataClassification untouched. Tests: unit (validator old+new vocab, `asset` rejected at route) + isolation (CHECKs old+new, RLS WITH CHECK on new vocab, resolver traversal over `asset`/infra edges). | **BUILT — pending review/commit** | `db/migrations/20260801_enterprise_relationships_asset_expansion.sql`; `enterpriseRelationshipValidation.ts`; `app/src/lib/enterpriseContext.ts`; `enterpriseRelationships.test.ts`; `enterpriseRelationshipsRls.test.ts`. **Resume-verified 2026-07-05:** working-tree diffs coherent + complete (migration, validator, app mirror, unit + isolation tests, ARCHITECTURE.md §2.4); unit tests 8/8 green locally; isolation tests CI-only (`TEST_DATABASE_URL`). Gate review RESOLVED by Simmee ruling 2026-07-05 (see Reconciliation notes). **DONE — PR #496, squash `fa5444c0`, branch deleted** | New relationship types reachable only via ECL routes (`SECURELOGIC_ENTERPRISE_CONTEXT_ENABLED`, off); `asset` endpoint additionally route-dark until registry Phase 1 |
| 1 | Phase 0 — contract + `asset_registry_v` view + flag-gated `GET /api/assets` | **DONE — PR #497, squash `e4537089`, branch deleted** | `20260802_asset_registry_view.sql` (first SQL VIEW in db/migrations — security_invoker guarded PG≥15 + app_request read grants); `assetRegistry.ts` (AssetType/AssetRef/CanonicalAsset/AssetTypeSpec truth table); `assetRegistryFeatureFlag.ts`; `routes/assets.ts` (GET /api/assets — flag→auth→org→capability→asTenant, mandatory org predicate); render.yaml flag ×4 engine blocks; unit (contract lockstep + handlers) + isolation (`assetRegistryView.test.ts`) | `SECURELOGIC_ASSET_REGISTRY_ENABLED` (declared "false"; default off). Reuses per-org `enterprise_context` capability — one commercial boundary, no second gating vocabulary |
| 2 | Phase 1 — `assets` spine + `asset_id` columns + `registerAsset()` + backfill; flip `asset` node type live at the route layer (`NODE_TYPES` + `NODE_TYPE_TABLE`) | **DONE — PR #498, squash `8868779`, branch deleted** | `20260803_assets_spine.sql` (Tier-0 table: identity-only per EAR-AD-2, UNIQUE (org,backing_kind,backing_id), RLS NOT FORCE + app_request DML, org FK CASCADE; nullable `asset_id` FK SET NULL on the 3 backing tables; idempotent in-migration backfill; view repointed — registry identity wins, COALESCE fallback keeps unregistered rows visible); `assetRegistrar.ts` (registerAsset/deregisterAsset/backfillAssetRegistry, joins caller tx via pg proxy); hooks on ALL 5 create paths (vendors POST + aiSystems POST wrapped in withTenant for atomicity, enterpriseEntities create, CSV import, templateLoader) + 2 delete paths (aiSystems DELETE, enterpriseEntities delete); `asset` node type live in `NODE_TYPES`/`NODE_TYPE_TABLE` + app mirror; `scripts/backfill-asset-registry.ts`; dataClassification `assets` entry; unit (registrar + migration lockstep) + isolation (`assetsSpine.test.ts`) | same. registerAsset/deregisterAsset are **flag-gated** — live routes gain zero writes while dark. **Operator action at enablement:** run `scripts/backfill-asset-registry.ts` once (idempotent) to register dark-window rows; until then the view's COALESCE fallback keeps them visible |
| 3 | Phase 2 — generalize `GRAPH_REPRESENTABLE` + matcher via asset-type spec | **DONE — PR #499, squash `c3614153`, branch deleted** (incl. graph_node BFS-alias fix caught by CI) | `20260804_phase2_asset_targets.sql` (nullable `asset_id` FK SET NULL on suggestions + assessments; 3 target_type CHECKs + `'asset'` — EAR-AD-3, quartet stops growing; `signal_asset_links` unification view, invoker-guarded + app_request SELECT); engine consumes `isGraphRepresentableTarget` (spec bridge) + new rule **R1b** (strong match on non-graph target → `affected`; R2 unchanged for vendor/ai_system); worker spec-driven neighborhood (asset targets seed at backing node) + persists `asset_id` (NOT content-hashed — hash still binds target_type/target_id); matcher: live vendor/ai_system branches spec-consulted (behavior-identical) + generic asset matcher (suggest-only, `assetRegistryEnabled()`-fenced) — §1.3 plane convergence; suggestions route: `asset` filter fenced on flag, `asset_registry_v` name enrichment, accept of asset targets → explicit 409 (**link-store shape = Phase 3 decision**); spec: application/database → riskTarget+name_canonical, entities-backed types → graphRepresentable. Tests: engine invariants (R1b/R2/spec-bridge), matcher source-asserts, writer param shift, isolation `phase2AssetTargets.test.ts` (exit criteria e2e: control-typed AND application-typed reach `affected` through the real worker; CHECK widening; FK SET NULL; view unification + app_request read) | same. New matchable behavior is double-fenced: matcher branch behind `SECURELOGIC_ASSET_REGISTRY_ENABLED`, engine/worker behind `SECURELOGIC_ENTERPRISE_CONTEXT_ENABLED` (both default off) |
| 4 | Phase 3 — new asset types + connector sync route/worker. **Split: 3a native types / 3b connector activation.** | **DONE — 3a: PR #500, squash `bd1bd09f`; 3b: PR #501, squash `8d7d5c3c` (incl. CI repairs: provenance 'connector', conflict-safe createDetailAsset); branches deleted** | 3a: `20260806_asset_detail_tables.sql` (4 Tier-1 detail tables — S0 typed columns + shared header + `external_ref` sync-dedup + RLS NOT FORCE + app_request DML; `assets.backing_kind` CHECK +4; `asset_registry_v` +4 arms); spec flips (4 new types → graphRepresentable via Tier-0 `asset` node, riskTarget + name_canonical); worker seeds detail-backed targets at their own `asset` node; generic matcher repointed at `asset_registry_v` (all 6 registry-matched types uniform); `assetDetailValidation.ts` + `assetDetailPersistence.ts` (`createDetailAsset` — insert+register one tx, per-type 10k cap, shared with 3b worker); unified `POST /api/assets` + `GET /api/assets/:id` (detail-backed types only — per-type routes stay authoritative, EAR-AD-1); dataClassification ×4; unit (validator + migration lockstep) + isolation (`assetDetailTables.test.ts`: round-trip create→register→view→graph-endpoint, dedup, RLS). **3a scope notes:** no update/delete routes yet (v1 create/read; follow-up), per-type cap constant operator-tunable later. 3b: `20260807_enterprise_connectors.sql` (per-org config table — config AES-256-GCM-encrypted at app layer via fieldEncryption, connector_id CHECK lockstep with REQUIRED_CONNECTOR_IDS, row-level `enabled` = the per-connector activation control [supersedes the "per-connector env flag" sketch — env can't scale per-org], RLS NOT FORCE + app_request DML) + `20260808_jobs_connector_sync.sql` (jobs CHECK +'connector_sync'); `connectorSyncCore.ts` (PURE category→lane mapping: endpoint/vuln 'asset'→endpoint detail, cloud 'asset'→cloud_resource detail (provider from config), cmdb→import lane, identity→accounts via import + one identity_system detail for the IdP itself; MAX 5000/sync, relationships deferred loudly — CSV parity); `connectorHttpClient.ts` (prod HttpClient: full A10-G1 SSRF defense on EVERY request — assertSafeWebhookUrl + pinned undici Agent, redirect:error, 30s timeout, 10MB cap, non-2xx bodies never surfaced); `connectorConfigStore.ts` (encrypt/decrypt/redact — routes echo config KEYS only, never values/ciphertext; secrets excluded from data exports via dataClassification exportExcludedColumns); `enterpriseImportPersistence.ts` (insertOne/existingKeys/capHeadroom EXTRACTED from the CSV route → shared with the worker; folds two dark-lane fixes: `identity` joined ENTERPRISE_TYPES [latent R7 bug — identity rows deduped/capped against ai_systems], enterprise-entity inserts persist `external_ref` + provenance 'connector' (pre-existing 20260718 CHECK value)); `routes/connectors.ts` (GET list merged w/ registry, PUT validate+encrypt+upsert, DELETE, POST :id/sync → 202 job enqueue deduped against pending); `workers/connectorSyncWorker.ts` (claim SKIP LOCKED elevated; fetch OUTSIDE tx; plan+persist+last_sync_*+succeeded in ONE withTenant tx; per-run summary; audit after commit); routes/index.ts + server.ts wiring. Tests: unit (mapping + migration lockstep + handler no-secret-echo + enqueue dedup — 16 new) + isolation `connectorSync.test.ts` (§4 Phase-3 EXIT: Defender sync → endpoint details + registry + view under mock credentials; re-sync idempotent; IdP → accounts + identity_system; unconfigured-org non-retryable fail; double-fence runOneTick; RLS) | same. Double-fenced: routes 404 + worker idle-skips unless BOTH `SECURELOGIC_ENTERPRISE_CONTEXT_ENABLED` AND `SECURELOGIC_ASSET_REGISTRY_ENABLED`; per-connector activation additionally requires the org row's `enabled=true`. No dark-window gap |
| 5 | Phase 4 — UI consolidation | **BUILT — in PR** | `app/src/components/assetKit.tsx` (cross-domain design-system kit: CriticalityBadge/StatusChip/MetaChip/TypeChip/ReadFailurePanel promoted from the ECL `shared.tsx`); per-type pages render via the kit — vendors + ai-systems local duplicate badge implementations DELETED and repointed (ai-systems deployment StatusChip stays local: different vocabulary), ECL `shared.tsx` re-exports the kit so all existing imports work unchanged; unified `/assets` page (app/src/app/assets/page.tsx — cross-type list over GET /api/assets with type filter chips, pagination, EAR-AD-1 deep links to authoritative per-type pages via `assetDetailHref`; detail-backed kinds unlinked v1); `app/src/lib/assetRegistry.ts` (pure vocabulary mirror + helpers) + `getAssets` wrapper in api.ts; nav: `asset_registry` NavFeatureFlag (fail-closed) + "Asset Registry" header link + layout env resolution + render.yaml app-block flag (two-switch model, app half); knowledge index regenerated. Tests: navigationFlags (dark-by-default + independence from ECL flag + entitlement gate), assetRegistry helpers; app typecheck + 116 unit + knowledge-index staleness green | Nav dark behind app-side `SECURELOGIC_ASSET_REGISTRY_ENABLED` (default "false" in render.yaml); page itself fails closed via engine 404/403 (two-switch) |
| 6 | Phase 5 — output completeness | **BUILT — in PR** | R6 stats endpoint (`enterpriseContextStats.ts`) + live `GET /api/dashboard/summary` (`dashboard.ts`) gain a registry-wide rollup over `asset_registry_v` (total + by asset_type [+ by criticality on stats]) — **flag-gated ADDITIVE key**: while `SECURELOGIC_ASSET_REGISTRY_ENABLED` is off both responses stay byte-identical (dashboard/summary is a LIVE route — additive-only was mandatory); app: `EnterpriseContextStats.assets?` optional mirror + exec-dashboard "Assets by type" section (renders only when the key is present → dark with zero UI change otherwise, deep-links to `/assets`). Posture engine untouched (§3.4 REUSE — consumes findings/risks, already asset-agnostic); gap/audit reports contain no asset counts (verified — nothing to repoint). **Optional Brief↔asset-applicability convergence: deliberately NOT built** (ARCHITECTURE.md marks it optional; platform-first discipline — revisit as a P6–P11 candidate). Tests: unit (flag-off → no key + 4 queries; flag-on → org-scoped `asset_registry_v` rollup) + isolation `phase5AssetRollups.test.ts` (§4 exit: total ≡ Σ by_type ≡ per-type table counts across vendor/ai_system/application/endpoint; dark-posture key absence; org-B zero isolation) | `stats.assets` / summary `assets` keys exist ONLY while `SECURELOGIC_ASSET_REGISTRY_ENABLED` is on (default off) |

## P6–P11 items (scope expansion 2026-07-06 — design authority: `docs/architecture/enterprise-asset-registry/P6-P11-ROADMAP.md`)

| # | Item | Status | Evidence | Flag / dark posture |
|---|---|---|---|---|
| P6 | Asset lifecycle completeness — PATCH/DELETE for detail-backed types + `/assets/[id]` page (closes the 3a v1 gap) | **BUILT — in PR** | `validateAssetDetailUpdate` (partial patch, create vocabularies, null-clears nullable fields, provider NOT NULL protected) + `updateDetailAsset`/`deleteDetailAsset` (conflict pre-checks — classified returns, never thrown 23505 [the 3b tx-abort lesson]; delete = detail row + `deregisterAsset` one tx); routes: `PATCH`/`DELETE /api/assets/:id` — registry-id resolution, detail-backed ONLY (vendor/ai_system/entity-backed → 409 `not_detail_backed`, EAR-AD-1), audit events; app: `/assets/[id]` read-only detail page (header + typed detail), `getAsset` wrapper, `assetDetailHref` homes detail kinds at `/assets/[id]` (closes Phase 4's unlinked-row note); knowledge index regenerated. Tests: unit (update validator + handler matrix, 18 green) + isolation `assetLifecycle.test.ts` (round-trip update→view read-through→delete; conflict classification; cross-org not_found; **EAR-AD-3 proof: suggestion `asset_id` NULLs on delete while `(target_type,target_id)` survives**) | same (`SECURELOGIC_ASSET_REGISTRY_ENABLED`, default off) |
| P7 | Connector relationship persistence (closes the 3b deferral) | **DONE — PR #505 (merged)** | Plan passes relationships through external_ref-keyed (self-edges dropped in the pure plane + counted — DB CHECK would abort the tx); worker resolves both endpoints by `(org, external_ref)` across enterprise_entities ('enterprise_entity' node) + 4 detail tables ('asset', asset_id — EAR-AD-4); `INSERT … ON CONFLICT DO NOTHING` vs the live-edge unique index (idempotent re-sync, never-thrown 23505); `enforceEnterpriseEdgeLimit` headroom respected; edges carry `note='connector:<id>'`; summary gains relationships_created/existing/unresolved/cap_skipped/self_dropped — no silent skips. Tests: unit (pass-through + self-drop) + isolation (CMDB mock sync → app→server depends_on edge + 1 unresolved counted; re-sync edge-idempotent) | same (double-fenced worker; ECL edge vocabulary only — no growth) |
| P8 | Track A — asTenant adoption on vendors/aiSystems/controls/obligations/actions + unwrapped assessment routes | **BUILT — in PR** | 46 endpoints across 10 route files wrapped in `asTenant` (vendors ×7, aiSystems ×5, controls ×5, obligations ×5, actions ×5, aiGovernanceAssessments ×4, controlAssessments ×4, obligationAssessments ×4, governanceReviews ×3, vendorReviews ×4) after a full per-endpoint safety survey: manual `pg.connect()` transactions become savepoints under the wrap (tenantContext savepoint proxy — verified); the only un-awaited helper anywhere is `writeAuditEvent` (pgElevated — off-scope, safe); every DB-error catch returns immediately or ROLLBACKs-to-savepoint. 3 inner `withTenant` blocks REMOVED (vendors POST, aiSystems POST + DELETE — withTenant doesn't nest; the wrap now owns the single tx, registry hooks stay atomic). 1 documented exception: `GET /vendors/export.csv` streams → stays unwrapped (buffering proxy = one status+json). New `tenantWrapCoverage.test.ts` source-asserts every endpoint is wrapped (endpoint-count drift fails CI) + no nested withTenant. Full suite 5241 green — behavior-identical (explicit org predicates remain the primary control; RLS armed as defense-in-depth on the tenant channel) | No flag — pure hardening; observable behavior unchanged until the operator repoints DATABASE_URL to app_request (existing A04-G1 phase-3 plan, NOT this PR) |
| P9 | Track C — gating dual-gate (additive; entitlement-leg removal = STOP GATE, product decision) | NOT STARTED | — | new flag, default off |
| P10 | Track B — generic asset-assessment service (requires its own design memo first) | NOT STARTED | — | dark |
| P11 | Output convergence (Brief cites asset applicability) + enablement runbook + final report | NOT STARTED | — | dark; runbook documents, never enables |
